# The production API origin. Leave blank for same-origin requests.
NEXT_PUBLIC_API_BASE_URL=

# Fixtures are loaded only when NODE_ENV is not "production" and this is true.
NEXT_PUBLIC_USE_CUSTOMER_FIXTURES=false

